import React from 'react';

const Intro = () => {
  return(
    <div style={styles.container}>
    </div>
    )
}

const styles = {
  container: {
    height: '30vh',
    width: '100%',
    backgroundColor: 'blue'
  }
}

export default Intro;